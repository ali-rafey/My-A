// Default content for the @modal parallel slot when no intercept is active (i.e. on the plain
// /blogs listing). Returning null means the slot renders nothing — the listing layout looks
// exactly as it did before the modal was wired up.
export default function ModalDefault() {
  return null;
}
