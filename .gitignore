# Legacy prototype (kept for reference; safe to delete after migration verified)
client/node_modules/
client/dist/
server/.venv/
server/__pycache__/
server/**/*.pyc
*.pyc

# Next.js
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts.bak

# Env (NEVER commit secrets)
.env
.env.local
.env*.local
.env.development
.env.test
.env.production
.env.local.example
README.md

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vercel
.vercel

# Claude Code worktrees (project-root .claude/ contains the AI's
# isolated checkout — never belongs in git history).
.claude/

# Supabase CLI local state (login tokens, project refs).
# NOTE: this is the LEADING-DOT folder created by the CLI. The
# supabase/ folder at the repo root (no dot) IS tracked because it
# holds schema.sql — the single source of truth for the DB.
.supabase/

# Build / dev caches
.swc/
.turbo/
.cache/

# Test coverage
coverage/

# Editor / scratch backups
*.bak
*~
.*.swp
.*.swo

# Catch-all logs
*.log

# macOS AppleDouble metadata
._*
